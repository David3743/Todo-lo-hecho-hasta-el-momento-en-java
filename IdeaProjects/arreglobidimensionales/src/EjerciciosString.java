import java.util.Locale;

public class EjerciciosString {
    public static void main (String[] args){
        // lenght me va a decir el tamaño de la cadena ( con espacios y tod$)
        //substring- extrae una parte del string, y funciona como las posiciones de un arreglo
        //toLowerCase-Vuleve tod$ dentro del arreglo en minuscula
        //toUpperCase - pone tod% el arreglo en mayuscula
        //contac(aca va variable a concatenar)-sirve para unir dos string
        //replace(colocar lo que quiere reemplazar, colocar por lo que lo quiere reemplazar)-sirve para reemplazar
        String texto = "Codigos de programacion";
        String variable = "hola";
        System.out.println(texto.charAt(0));

    }
}
